{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Configuration",
  "description": "The configuration format for the statements CLI tool",
  "type": "object",
  "properties": {
    "flags": {
      "description": "Flags to provide to the CLI's process command",
      "$ref": "./_flags.schema.json"
    }
  },
  "required": [
    "flags"
  ]
}
